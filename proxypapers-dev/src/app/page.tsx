// Landing page. Sections are composed here as they are ported
// (Hero first, then the sections below it). Placeholder until Step 1.
export default function Home() {
  return (
    <main
      style={{
        minHeight: "100vh",
        display: "grid",
        placeItems: "center",
      }}
    >
      <p>Proxy Papers — port in progress.</p>
    </main>
  );
}
